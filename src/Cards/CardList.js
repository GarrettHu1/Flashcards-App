import React, { useEffect, useState } from "react";
import {
  Link,
  NavLink,
  Route,
  Switch,
  useParams,
  useRouteMatch,
  useHistory,
} from "react-router-dom";

export default function CardList() {
  return (
  <div>
    list of cards for (cardId)
    </div>
  )
}